@model IEnumerable<DataBase_RH_BanderaBlanca.Models.dias>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Dia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Dia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idDias }) |
            @Html.ActionLink("Details", "Details", new { id=item.idDias }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idDias })
        </td>
    </tr>
}

</table>
