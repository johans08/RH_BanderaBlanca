@model IEnumerable<DataBase_RH_BanderaBlanca.Models.catalogo_tipo_liquidaciones>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Liquidaciones)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Liquidaciones)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCatalogo_Tipo_Liquidaciones }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCatalogo_Tipo_Liquidaciones }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCatalogo_Tipo_Liquidaciones })
        </td>
    </tr>
}

</table>
