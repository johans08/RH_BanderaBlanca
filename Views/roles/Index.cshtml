@model IEnumerable<DataBase_RH_BanderaBlanca.Models.roles>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion_Rol)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion_Rol)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRol }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRol }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRol })
        </td>
    </tr>
}

</table>
