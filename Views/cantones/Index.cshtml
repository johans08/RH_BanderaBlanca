@model IEnumerable<DataBase_RH_BanderaBlanca.Models.cantones>

@{
    ViewBag.Title = "Gestión de Cantones";
}

<div class="row">
    <div class="col-12 col-lg-12 col-xxl-12 d-flex">
        <div class="card flex-fill">
            <div class="card-header">
                <h5 class="card-title mb-0">Catálogo de Cantones</h5>
            </div>

            <!-- Contenedor principal -->
            <div class="container mt-4 pb-5">

                <!-- Encabezado con botón para crear -->
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2 class="mb-0">Lista de Cantones</h2>
                    <a href="@Url.Action("Create")" class="btn btn-success">Crear Nuevo Cantón</a>
                </div>

                <!-- Tabla responsiva -->
                <div class="table-responsive">
                    <table class="table my-0 table-hover">
                        <thead class="thead-light">
                            <tr>
                                <th>@Html.DisplayNameFor(model => model.Nombre_Canton)</th>
                                <th>@Html.DisplayNameFor(model => model.provincias.Nombre_Provincia)</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(modelItem => item.Nombre_Canton)</td>
                                    <td>@Html.DisplayFor(modelItem => item.provincias.Nombre_Provincia)</td>
                                    <td class="text-right">

                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
</div>
