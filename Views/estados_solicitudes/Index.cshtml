@model IEnumerable<DataBase_RH_BanderaBlanca.Models.estados_solicitudes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Estados_Solicitud)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Estados_Solicitud)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idEstados_Solicitudes }) |
            @Html.ActionLink("Details", "Details", new { id=item.idEstados_Solicitudes }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idEstados_Solicitudes })
        </td>
    </tr>
}

</table>
