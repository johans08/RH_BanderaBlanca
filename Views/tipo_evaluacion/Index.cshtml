@model IEnumerable<DataBase_RH_BanderaBlanca.Models.tipo_evaluacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Evaluacion1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Evaluacion1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTipo_Evaluacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTipo_Evaluacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTipo_Evaluacion })
        </td>
    </tr>
}

</table>
